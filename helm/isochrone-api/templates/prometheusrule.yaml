{{ if .Values.createResource.prometheusRule }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  labels:
    prometheus: k8s
    role: alert-rules
  name: {{ .Values.envName}}-isochrone-api-platform-pods
  namespace: {{ .Release.Namespace }}
spec:
  groups:
    - name: PlatformPodsUnavailable
      rules:
        - alert: {{ camelcase .Values.envName }}-PlatformPodsUnavailable-IsochroneApi
          annotations:
            message: 'There are {{ "{{$value}}" }} Ready pods in {{ .Values.envName }} Isochrone API'
            summary: There are less Ready pods than required
          expr: |
            kube_deployment_status_replicas_ready{deployment="{{ .Values.envName }}-isochrone-api"}
            < 1
          for: {{ .Values.alertingTime }}
          labels:
            severity: critical
---
{{ end }}